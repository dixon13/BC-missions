if (!isServer) exitWith {};

[] call completedObjective;

// Change overcast weather
//_randOvercast = (random 100) / 100;
//50 setOvercast _randOvercast;

// Change time of day
//skiptime (random 24);